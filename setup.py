from setuptools import setup,find_packages

setup(
    name='Hillsoft Labs',
    version='0.0.1',
    author='Sharath Thirunagaru',
    author_email='sharath.tds@gmail.com',
    description="Hillsoft Web Application for timesheets management",
    packages=find_packages(exclude=[])
)